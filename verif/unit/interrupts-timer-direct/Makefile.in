
TEST_NAME = interrupts-timer-direct
TEST_SRC  = $(UNIT_ROOT)/$(TEST_NAME)/test_interrupts.c \
            $(UNIT_ROOT)/$(TEST_NAME)/test_interrupts.S

$(eval $(call add_unit_test,$(TEST_NAME),$(TEST_SRC)))

