
TEST_NAME = interrupts-timer-vectored
TEST_SRC  = $(CORE_UNIT_ROOT)/$(TEST_NAME)/test_interrupts.c \
            $(CORE_UNIT_ROOT)/$(TEST_NAME)/test_interrupts.S

$(eval $(call add_unit_test,$(TEST_NAME),$(TEST_SRC)))

